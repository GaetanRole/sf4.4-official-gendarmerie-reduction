{% extends 'base.html.twig' %}

{% block title 'admin.reduction.edit.head.meta.title'|trans %}

{% block og_title 'admin.reduction.edit.head.meta.title'|trans %}

{% block body_id 'admin-opinion-edit' %}

{% block body %}
    <section>
        <div class="container">
            <div class="row mb-4">
                <div class="col">
                    <h1>{{ 'admin.opinion.edit.body.h1'|trans }}</h1>

                    {{ include('opinion/_form.html.twig', {'button_label': 'Update'}) }}

                    {{ include('admin/opinion/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
