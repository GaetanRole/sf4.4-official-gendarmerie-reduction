{% extends 'base.html.twig' %}

{% block title %}{{ 'admin.category.new.head.meta.title'|trans }}{% endblock %}

{% block body_id 'admin-category-new' %}

{% block body %}
    <section>
        <div class="container">
            <div class="row mb-4">
                <div class="col">
                    <h1>{{ 'admin.category.new.body.h1'|trans }}</h1>

                    {{ include('admin/category/_form.html.twig') }}

                    <a href="{{ path('app_admin_category_index') }}">{{ 'link.admin.category.index'|trans }}</a>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
