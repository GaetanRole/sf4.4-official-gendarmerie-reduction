{% extends 'base.html.twig' %}

{% block og_title 'user.admin.new.head.meta.title'|trans %}
{% block title 'user.admin.new.head.meta.title'|trans %}

{% block content_id 'user-admin-new' %}
{% block content %}
    <section>
        <div class="container">
            <div class="row mb-4">
                <div class="col">
                    <h1>{{ 'user.admin.new.body.h1'|trans }}</h1>

                    {{ include('user/admin/_form.html.twig') }}

                    <a href="{{ path('app_admin_user_index') }}">{{ 'link.admin.user.index'|trans }}</a>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
