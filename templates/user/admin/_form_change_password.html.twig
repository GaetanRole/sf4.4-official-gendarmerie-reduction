{{ form_start(formChangePassword) }}
    {{ form_widget(formChangePassword) }}
<button class="btn btn-sm btn-dark"><i class="fas fa-check"></i>&nbsp;{{ 'btn.save'|trans }}</button>
{{ form_end(formChangePassword) }}
