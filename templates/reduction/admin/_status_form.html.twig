<form method="post" class="mt-2" action="{{ path('app_admin_reduction_change_status', {'slug': reduction.slug}) }}" onsubmit="return confirm('{{ 'admin.notification.confirm.status'|trans }}');">
    <input type="hidden" name="_method" value="PUT">
    <input type="hidden" name="_token" value="{{ csrf_token('status' ~ reduction.slug) }}">
    <button class="btn btn-sm btn-warning"><i class="fas fa-sync"></i>&nbsp;{{ 'btn.status'|trans }}</button>
</form>
