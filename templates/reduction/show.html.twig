{% extends 'base.html.twig' %}

{% block title %}Reduction{% endblock %}

{% block body %}
    <h1>Reduction</h1>

    {% include 'default/_flash_messages.html.twig' %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reduction.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ reduction.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ reduction.email }}</td>
            </tr>
            <tr>
                <th>ClientIp</th>
                <td>{{ reduction.clientIp }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ reduction.title }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ reduction.slug }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reduction.description }}</td>
            </tr>
            <tr>
                <th>Region</th>
                <td>{{ reduction.region }}</td>
            </tr>
            <tr>
                <th>Department</th>
                <td>{{ reduction.department }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ reduction.city }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ reduction.creationDate ? reduction.creationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>IsBigDeal</th>
                <td>{{ reduction.isBigDeal ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ reduction.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reduction_index') }}">back to list</a>

    <a href="{{ path('reduction_edit', {'id': reduction.id}) }}">edit</a>

    {{ include('admin/reduction/_delete_form.html.twig') }}
{% endblock %}
